apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # pinned so provisioned dashboards bind reliably
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
